{
  "$schema": "https://railway.app/railway.schema.json",
  "build": {
    "builder": "NIXPACKS"
  },
  "deploy": {
    "startCommand": "uv run python railway_entrypoint.py",
    "healthcheckPath": "/api/status",
    "healthcheckTimeout": 60,
    "restartPolicyType": "ON_FAILURE"
  }
}

